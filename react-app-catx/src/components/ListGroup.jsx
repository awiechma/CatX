import React from "react";

// creates listgroup 
const ListGroup = () => {
    return (
        <ul className="list-group list-group-flush">
            
            
        </ul>
    );
};

export default ListGroup;